// The PortalShell redirects /portal to the signed-in user's role home
// (admin/donor/trainer). This page only exists as the redirect anchor.
export default function PortalIndexPage() {
  return null;
}
